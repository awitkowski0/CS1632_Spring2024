package edu.pitt.cs;

import static org.junit.Assert.*;
import org.mockito.*;

import org.junit.Test;

public class AdderTest {

	/**
	 * Suppose you are testing a Java package Adder.jar which has the following
	 * requirements:
	 * 
	 * The program shall accept two integer commandline arguments.
	 * The program shall print "Positive numbers only please." if either of the
	 * arguments is less than or equal to 0.
	 * The program shall print out the sum of the two arguments if both of the
	 * arguments are positive integers.
	 * Suppose the main method of Adder.jar looks like the following:
	 */
	@Test
	public void testAdder() {
		
	}
}

